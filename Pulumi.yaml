name: infrastructure
runtime: nodejs
options:
  refresh: always
config:
  digitalocean:token:
    value: digitalocean:token
    secret: true
template:
  config:
    minNodeCount:
      default: 1
    maxNodeCount:
      default: 3
    argocdAdminPasswordBcryptHash:
      default: argocdAdminPasswordBcryptHash
      secret: true
    githubUsername:
      default: githubUsername
    githubPassword:
      default: githubPassword
      secret: true
